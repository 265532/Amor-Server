<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smartAmor.mapper.AmorphousAlloysMapper">
    <update id="update">
        UPDATE amorphous_alloys
        SET
        name = #{name},
        version = #{version},
        base_type_id = #{baseTypeId},
        formula = #{formula},
        properties = #{properties, typeHandler=com.smartAmor.typeHandler.PropertiesTypeHandler},
        updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>
    <insert id="insert">
        INSERT INTO amorphous_alloys (id, name, version, base_type_id, formula, properties, created_at, updated_at)
        VALUES (#{id}, #{name}, #{version}, #{baseTypeId}, #{formula},
        #{properties, typeHandler=com.smartAmor.typeHandler.PropertiesTypeHandler}, #{createdAt}, #{updatedAt})
    </insert>
</mapper>